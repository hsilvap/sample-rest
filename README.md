# sample-rest
Sample REST written in node.js

# To run this app do the following

open a terminal
run `$ npm install`

then `$ npm run start`


# To test it:

in a terminal do the following
run `$ npm run test`
